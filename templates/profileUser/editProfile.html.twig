{% extends 'base.html.twig' %}

{% block title %}Hello you!{% endblock %}

{% block body %}


<h1>Edit my profile</h1>

    {{ form_start(editEmailForm) }}
            
        {{ form_row(editEmailForm.email) }}

        {{ form_errors(editEmailForm) }}

        <button type="submit">
            Enregistrer le nouvel email
        </button>

    {{ form_end(editEmailForm) }}


{% endblock %}
