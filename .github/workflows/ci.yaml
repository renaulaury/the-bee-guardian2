name: CI Symfony 7.3 - Quality Control
on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3' # On prend la plus stable pour Symfony 7.3
          extensions: mbstring, xml, ctl, iconv, intl
          coverage: none

      - name: Check Composer Lock
        run: composer validate --strict

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install Dependencies
        run: composer install --no-progress --prefer-dist --optimize-autoloader

      - name: Lint Symfony Config
        run: |
          php bin/console lint:yaml config
          php bin/console lint:twig templates
          php bin/console lint:container

      - name: Setup Database (SQLite for Speed)
        env:
          DATABASE_URL: "sqlite:///%kernel.project_dir%/var/data.db"
        run: |
          php bin/console doctrine:database:create --env=test
          php bin/console doctrine:migrations:migrate --no-interaction --env=test

      - name: Run PHPUnit Tests
        env:
          DATABASE_URL: "sqlite:///%kernel.project_dir%/var/data.db"
        run: php bin/phpunit

      - name: Security Check
        run: |
          curl -sS https://get.symfony.com/cli/installer | bash
          /home/runner/.symfony5/bin/symfony check:security